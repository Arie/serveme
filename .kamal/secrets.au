# AU database — system Postgres 15 on the host. pg_hba already has
# `host all all 127.0.0.1/32 trust`, which covers the docker bridge —
# no changes needed.
DATABASE_URL=postgres://serveme@host.docker.internal/serveme_production
