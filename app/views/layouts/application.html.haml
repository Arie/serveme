!!!
%head
  %title serveme.tf - free TF2 server reservations
  = stylesheet_link_tag    "application", :media => "all"
  = javascript_include_tag "application"
  = csrf_meta_tags
  = render 'shared/google_analytics'
  = content_for(:head)
%body

  = render 'shared/navigation'

  .container-fluid

    - unless donator?
      .row
        .span11
          = render 'shared/adsense_header'

    = render 'shared/nag_message' if current_user

    .row
      .span11.alert.alert-error
        One of our machines got attacked, so our server fleet is missing some sailors tonight

    = render 'shared/flash'

    = yield

  = render 'shared/timezone'
