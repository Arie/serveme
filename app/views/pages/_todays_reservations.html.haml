%h3 Current reservations
- if @reservations.any?
  %table.table.table-condensed
    %thead
      %tr
        %th Server
        %th Reserved by
        %th Reserved from
        %th Reserved until
    %tbody
      - @reservations.each do |reservation|
        
        - klass = 'success' if reservation.active?
        - klass = 'info'    if reservation.future?
        %tr{:class => klass}
          %td= reservation.server.name
          %td= reservation.user.nickname
          %td= I18n.l(reservation.starts_at,  :format => :time)
          %td= I18n.l(reservation.ends_at,    :format => :time)

- else
  = "None yet!"
