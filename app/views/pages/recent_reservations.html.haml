%h2 Recent reservations

.row
  .span10

    %table.table.table-hover.table-bordered.table-striped
      %thead
        %tr
          %th Who
          %th What
          %th When
      - @recent_reservations.each do |reservation|
        - user    = @users.select   {|u| u.id == reservation.whodunnit.to_i }.first
        %tr
          %td= user.try(:name)
          %td= I18n.l(reservation.created_at, :format => :long)
