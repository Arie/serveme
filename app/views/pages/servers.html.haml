%h2 Servers

.row
  .span10
    %table.table.table-hover.table-bordered.table-striped
      %thead
        %tr
          %th Name
          %th Map name
          %th Players
          %th FPS
          %th CPU%
          %th Traffic in
          %th Traffic out
          %th Uptime
      - @servers.each do |server|
        = render 'server_info', :server => server
