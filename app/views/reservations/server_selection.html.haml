.row
  .span12
    %h3 Pick a date and time...
    = render :partial => 'time_selection'

.row
  .span10
    %h3 ...or pick a server
    = render :partial => 'servers_reservations', :locals => { :servers => reservable_servers_for_user}
