%h2 New server config

.row
  .col-md-9
    = simple_form_for(@server_config, url: url_for(controller: "server_configs", action: "create"), :html => { :class => 'form' }) do |f|
      = f.input :file, label: "File name (without .cfg)"
      = f.input :hidden

      .col-md-offset-2
        %button.btn.btn-default{:type => :submit}
          Save
