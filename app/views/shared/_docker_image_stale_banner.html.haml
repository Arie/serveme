- if DockerImageReadiness.stale?
  .row
    .col-md-12
      .alert.alert-info
        %i.fa.fa-info-circle
        A TF2 update was just released. On-demand (cloud) servers are temporarily unavailable while the server image is rebuilt — this usually takes a few minutes. Regular servers are unaffected.
